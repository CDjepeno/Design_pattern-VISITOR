import { App } from '../src/App';
describe('App run', () => {
  it('should return ', () => {
    expect(App.main1()).toBe('Cordonnée du rectangle')
  })
  it('should return ', () => {
    expect(App.main2()).toBe('Cordonnée du cercle')
  })
})
